{
  "name": "burnedpunks",
  "version": "1.0.0",
  "description": "Static site for burnedpunks.com — registry of burned CryptoPunks.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "fetch-svgs": "node scripts/fetch-punk-svgs.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "viem": "^2.21.0"
  }
}
